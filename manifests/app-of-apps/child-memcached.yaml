apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: child-memcached
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/adriankabala/argocd-v2-demo.git
    targetRevision: master
    path: manifests/apps/memcached
  destination:
    name: loft-default-virtualcluster-sd-test-argocd
    namespace: child-memcached
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
