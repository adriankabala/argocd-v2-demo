apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: child-pause
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/adriankabala/argocd-v2-demo.git
    targetRevision: master
    path: manifests/apps/pause
  destination:
    name: loft-default-virtualcluster-sd-test-argocd
    namespace: child-pause
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
