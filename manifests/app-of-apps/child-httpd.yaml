apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: child-httpd
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/adriankabala/argocd-v2-demo.git
    targetRevision: master
    path: manifests/apps/httpd
  destination:
    name: loft-default-virtualcluster-sd-test-argocd
    namespace: child-httpd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
